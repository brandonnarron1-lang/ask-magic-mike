# Resend event acceptance report

Local contract tests pass for signature rejection, verified delivery-event persistence, communication-event linkage, delayed-delivery handling, duplicate replay, complaint-driven suppression, and terminal bounce handling. The protected notification center now renders the latest provider lifecycle event and timestamp from safe metadata. No Production webhook secret was exposed or inferred.

The Phase 7 migration is applied in Production. One approved Brandon-only test was accepted with provider message ID `871e5b96-a10b-492a-bb23-9898824f0cd3`; it requested no Mike, consumer, BCC, or SMS delivery. Although the existing API key is send-scoped and returned HTTP 401 from the retrieve endpoint, authenticated Resend dashboard inspection showed `sent` and `delivered` events at 10:50 AM. The authorized Gmail inbox independently contained the message, so delivery is now claimed with provider-side and recipient-side evidence.

Production webhook acceptance passed on 2026-08-16. Resend webhook `d466d4d9-6837-49ae-9343-86c54c2bd720` is enabled for the documented eight-event allowlist and points to `https://www.askmagicmike.com/api/webhooks/email/events`. Its provider-issued signing secret is stored only as the Sensitive, Production-scoped Vercel variable `RESEND_WEBHOOK_SECRET`; the value was never printed, committed, or added to Preview. `RESEND_WEBHOOK_ENABLED` is now `true` in Production.

The deployed route on Vercel deployment `dpl_5g43rkAatsVi3FHyarZf7Km1jZfG` rejected an invalid signature with HTTP 400 and `invalid_signature`. A no-PII synthetic `email.sent` acceptance payload signed with the actual Resend-issued secret returned HTTP 200 with `duplicate=false`; exact replay returned HTTP 200 with `duplicate=true`. Neon Production contains exactly one matching `provider_webhook_events` row for event ID `msg_phase7_live_acceptance_1786914537362`, with `signature_verified=true`, payload hash `b3e5af1b0f0861316d70c77da0f04db6fac5d9830b3135d33ca00c18b114cd32`, and `processing_status=ignored` because the deliberately synthetic provider message ID matched no notification. This proves deployed signature alignment, minimized durable storage, and replay idempotency; it is not evidence of an actual provider-delivered email.

The lifecycle hardening shipped through PR 161 at `8f7697de5a7bf3384fb657fd5d0bbc31115dd6ad`. The enabled acceptance deployment passed public smoke (19/19 with two intentional skips), health, funnel, isolation, and runtime-log checks. No email, BCC, SMS, Push notification, consumer acknowledgment, lead, or notification row was created by the signed acceptance.
