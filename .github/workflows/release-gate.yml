name: Ask Magic Mike Release Gate

on:
  pull_request:
  push:
    branches:
      - platform/phase-2-release-hardening
      - main
  workflow_dispatch:

jobs:
  local-release-gate:
    name: local-release-gate
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Release doctor
        run: pnpm run release:doctor

      - name: Release safety scan
        run: pnpm run release:safety

      - name: Unit tests
        run: pnpm run test

      - name: Typecheck
        run: pnpm run typecheck

      - name: Lint
        run: pnpm run lint

      - name: Build
        run: pnpm run build

      - name: Assert canonical route manifest and cron targets
        run: pnpm run routes:assert

      - name: Release candidate report
        run: pnpm run release:report

      - name: Launch authority report
        run: pnpm run launch:authority

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: release-gate-artifacts
          path: |
            artifacts/*.json
            artifacts/*.md
          if-no-files-found: ignore
