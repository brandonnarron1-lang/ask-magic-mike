// The reviewed implementation remains in the preserved src/app inventory.
// This root wrapper is the only activation required by the checked-in Vercel
// cron and keeps root app/ as the canonical router.
export { GET } from "../../../../../src/app/api/admin/sla/sweep/route";
