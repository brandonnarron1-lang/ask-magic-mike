// Root `app` is the canonical Next.js router. Keep the reviewed handler in one
// implementation module while making the documented Lead Center API active.
export { GET } from "../../../../src/app/api/admin/leads/route";
