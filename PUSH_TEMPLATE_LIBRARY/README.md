# Phase 7 push template library

The canonical push registry contains internal-only, permission-gated previews. Push endpoints and device subscription material are prohibited from release artifacts. No consumer push or Mike test delivery is enabled.
